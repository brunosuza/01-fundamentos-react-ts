import styles from './Header.module.css';

export function Header() {
    return (
        <header className={styles.header}>          
            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/elixir/elixir-plain.svg" />
        </header>
    );
}